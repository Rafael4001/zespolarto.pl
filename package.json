{
    "private": true,
    "scripts": {
        "docker:dev": "docker-compose -f docker-compose.yml -f docker-compose.yml up --remove-orphans",
        "docker:prod": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml up --remove-orphans",
        "docker:clear": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml down"
    }
}
